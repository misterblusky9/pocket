package com.misterblusky9.pocket.entity;

public interface PrimedTntScaleAccess {
    double pocket$litScale();
    double pocket$dimensionScale();
    double pocket$renderScale(float partialTick);
}
